apiVersion: v1
kind: Service
metadata:
  name: taskflow-api-svc
  namespace: taskflow-prod
spec:
  type: NodePort
  selector:
    app: taskflow-api
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
